PORT=3000
API_KEY=secret123

# Database (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=tasks

# Auth (JWT). Generate a real random secret in production.
JWT_SECRET=dev-super-secret-change-me
JWT_EXPIRES_IN=1h
# Dev-only seed admin (remove in production)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin12345
